meta {
  name: Create Asset
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/assets
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "New Asset",
    "type": "image",
    "status": "ACTIVE"
  }
}
