meta {
  name: Update User
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/users/user_id_here
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "role": "editor"
  }
}
